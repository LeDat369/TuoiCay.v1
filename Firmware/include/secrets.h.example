/**
 * @file secrets.h.example
 * @brief Template for secrets.h - Copy this file to secrets.h and fill in values
 * 
 * WARNING: secrets.h should NEVER be committed to version control!
 * Add "secrets.h" to .gitignore
 * 
 * INSTRUCTIONS:
 * 1. Copy this file to secrets.h
 * 2. Fill in your WiFi and MQTT credentials
 * 3. Make sure secrets.h is in .gitignore
 */

#ifndef SECRETS_H
#define SECRETS_H

//=============================================================================
// WIFI CREDENTIALS
//=============================================================================
#define WIFI_SSID           "YOUR_WIFI_SSID"
#define WIFI_PASSWORD       "YOUR_WIFI_PASSWORD"

//=============================================================================
// MQTT BROKER
//=============================================================================
#define MQTT_BROKER         "192.168.1.100"     // MQTT broker IP/hostname
#define MQTT_PORT           1883                // MQTT port (1883 plain, 8883 TLS)
#define MQTT_USERNAME       ""                  // Leave empty if no auth
#define MQTT_PASSWORD       ""                  // Leave empty if no auth

//=============================================================================
// OTA PASSWORD (optional)
//=============================================================================
#define OTA_PASSWORD        "your_ota_password"

#endif // SECRETS_H
